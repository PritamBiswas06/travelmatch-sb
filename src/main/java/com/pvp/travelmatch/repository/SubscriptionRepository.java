package com.pvp.travelmatch.repository; import com.pvp.travelmatch.entity.*; import org.springframework.data.jpa.repository.JpaRepository; import java.time.LocalDateTime; import java.util.Optional;
import java.util.List; public interface SubscriptionRepository extends JpaRepository<Subscription,Long>{ Optional<Subscription> findByUserId(Long userId); List<Subscription> findByUserIdIn(List<Long> userIds); long countByPlan(SubscriptionPlan p); long countByStatus(SubscriptionStatus s); long countByStatusAndEndDateAfter(SubscriptionStatus s, LocalDateTime n); }
